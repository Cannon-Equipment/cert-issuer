apiVersion: v1
kind: Service
metadata:
  name: {{ include "cert-manager-issuer.fullname" . }}
  labels:
    {{- include "cert-manager-issuer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cert-manager-issuer.selectorLabels" . | nindent 4 }}
